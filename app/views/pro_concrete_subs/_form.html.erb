<%= form_with(model: pro_concrete_sub, local: true) do |form| %>
  <% if pro_concrete_sub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pro_concrete_sub.errors.count, "error") %> prohibited this pro_concrete_sub from being saved:</h2>

      <ul>
      <% pro_concrete_sub.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pro_concrete_id %>
    <%= form.text_field :pro_concrete_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
