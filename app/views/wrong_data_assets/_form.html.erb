<%= form_with(model: wrong_data_asset, local: true) do |form| %>
  <% if wrong_data_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wrong_data_asset.errors.count, "error") %> prohibited this wrong_data_asset from being saved:</h2>

      <ul>
      <% wrong_data_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :data_asset_id %>
    <%= form.text_field :data_asset_id %>
  </div>

  <div class="field">
    <%= form.label :checklist_id %>
    <%= form.text_field :checklist_id %>
  </div>

  <div class="field">
    <%= form.label :employee_now_id %>
    <%= form.text_field :employee_now_id %>
  </div>

  <div class="field">
    <%= form.label :employee_should_id %>
    <%= form.text_field :employee_should_id %>
  </div>

  <div class="field">
    <%= form.label :site_now_id %>
    <%= form.text_field :site_now_id %>
  </div>

  <div class="field">
    <%= form.label :site_should_id %>
    <%= form.text_field :site_should_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
