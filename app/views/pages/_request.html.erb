<div class="m-portlet m-portlet--full-height ">
	<div class="m-portlet__head">
		<div class="m-portlet__head-caption">
			<div class="m-portlet__head-title">
				<span class="m-portlet__head-icon">
        <i class="flaticon-transport m--font-brand"></i>
      	</span>
				<h3 class="m-portlet__head-text">
					Equipment Requests
				</h3>
				<span class="m-portlet__head-icon pl-2">
        	<%= link_to home_pages_path(p_name: 'request'), remote: true do %>
	        	<i class="flaticon-refresh m--font-danger"></i>
	        <% end %>
      	</span>
			</div>
		</div>
		<div class="m-portlet__head-tools">
			<ul class="nav nav-pills nav-pills--brand m-nav-pills--align-right m-nav-pills--btn-pill m-nav-pills--btn-sm" role="tablist">

				<% if current_user.role?(:assets_province) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link active" data-toggle="tab" href="#request_tab1_content" role="tab">
							Opened &nbsp; 
							<% if @request_assets.by_province_user_0(current_user.id).count > 0 %>
							<span class="m-badge m-badge--default"><%= @request_assets.by_province_user_0(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>
				<% if current_user.role?(:assets_site) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link active" data-toggle="tab" href="#request_tab2_content" role="tab">
							Opened &nbsp;
							<% if @request_assets.by_site_user_0(current_user.id).count > 0 %>
							<span class="m-badge m-badge--default"><%= @request_assets.by_site_user_0(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>

				<% if current_user.role?(:assets_manager) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link active" data-toggle="tab" href="#request_tab3_content" role="tab">
							Pass &nbsp;
							<% if @request_assets.pass.count > 0 %>
							<span class="m-badge m-badge--default"><%= @request_assets.pass.count %></span>
							<% end %>
						</a>
					</li>
				<% end %>
				<% if current_user.role?(:assets_province) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab4_content" role="tab">
							Pass &nbsp;
							<% if @request_assets.by_province_user_1(current_user.id).count > 0 %>
							<span class="m-badge m-badge--success"><%= @request_assets.by_province_user_1(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>
				<% if current_user.role?(:assets_site) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab5_content" role="tab">
							Pass &nbsp;
							<% if @request_assets.by_site_user_1(current_user.id).count > 0 %>
							<span class="m-badge m-badge--success"><%= @request_assets.by_site_user_1(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>

				<% if current_user.role?(:assets_province) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab6_content" role="tab">
							Not Pass &nbsp;
							<% if @request_assets.by_province_user_2(current_user.id).count > 0 %>
							<span class="m-badge m-badge--danger"><%= @request_assets.by_province_user_2(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>
				<% if current_user.role?(:assets_site) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab7_content" role="tab">
							Not Pass &nbsp;
							<% if @request_assets.by_site_user_2(current_user.id).count > 0 %>
							<span class="m-badge m-badge--danger"><%= @request_assets.by_site_user_2(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>

				<% if current_user.role?(:assets_manager) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab8_content" role="tab">
							Accepted &nbsp;
							<% if @request_assets.accepted.count > 0 %>
							<span class="m-badge m-badge--success"><%= @request_assets.accepted.count %></span>
							<% end %>
						</a>
					</li>
				<% end %>
				<% if current_user.role?(:assets_province) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab9_content" role="tab">
							Accepted &nbsp;
							<% if @request_assets.by_province_user_3(current_user.id).count > 0 %>
							<span class="m-badge m-badge--success"><%= @request_assets.by_province_user_3(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>
				<% if current_user.role?(:assets_site) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab10_content" role="tab">
							Accepted &nbsp;
							<% if @request_assets.by_site_user_3(current_user.id).count > 0 %>
							<span class="m-badge m-badge--success"><%= @request_assets.by_site_user_3(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>

				<% if current_user.role?(:assets_manager) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab11_content" role="tab">
							Refused &nbsp;
							<% if @request_assets.refused.count > 0 %>
							<span class="m-badge m-badge--danger"><%= @request_assets.refused.count %></span>
							<% end %>
						</a>
					</li>
				<% end %>
				<% if current_user.role?(:assets_province) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab12_content" role="tab">
							Refused &nbsp;
							<% if @request_assets.by_province_user_4(current_user.id).count > 0 %>
							<span class="m-badge m-badge--danger"><%= @request_assets.by_province_user_4(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>
				<% if current_user.role?(:assets_site) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab13_content" role="tab">
							Refused &nbsp;
							<% if @request_assets.by_site_user_4(current_user.id).count > 0 %>
							<span class="m-badge m-badge--danger"><%= @request_assets.by_site_user_4(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>

				<% if current_user.role?(:assets_manager) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab14_content" role="tab">
							Ready &nbsp;
							<% if @request_assets.ready.count > 0 %>
							<span class="m-badge m-badge--success"><%= @request_assets.ready.count %></span>
							<% end %>
						</a>
					</li>
				<% end %>
				<% if current_user.role?(:assets_province) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab15_content" role="tab">
							Ready &nbsp;
							<% if @request_assets.by_province_user_5(current_user.id).count > 0 %>
							<span class="m-badge m-badge--success"><%= @request_assets.by_province_user_5(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>
				<% if current_user.role?(:assets_site) %>
					<li class="nav-item m-tabs__item">
						<a class="nav-link m-tabs__link" data-toggle="tab" href="#request_tab16_content" role="tab">
							Ready &nbsp;
							<% if @request_assets.by_site_user_5(current_user.id).count > 0 %>
							<span class="m-badge m-badge--success"><%= @request_assets.by_site_user_5(current_user.id).count %></span>
							<% end %>
						</a>
					</li>
				<% end %>


				<% if current_user.role?(:assets_manager) %>
				<li class="nav-item m-tabs__item">
					<a class="nav-link m-tabs__link " data-toggle="tab" href="#request_tab17_content" role="tab">
						Received &nbsp;
						<% if @request_assets.received.count > 0 %>
						<span class="m-badge m-badge--success"><%= @request_assets.received.count %></span>
						<% end %>
					</a>
				</li>
				<% end %>
				<% if current_user.role?(:assets_province) %>
				<li class="nav-item m-tabs__item">
					<a class="nav-link m-tabs__link " data-toggle="tab" href="#request_tab18_content" role="tab">
						Received &nbsp;
						<% if @request_assets.by_province_user_6(current_user.id).count > 0 %>
						<span class="m-badge m-badge--success"><%= @request_assets.by_province_user_6(current_user.id).count %></span>
						<% end %>
					</a>
				</li>
				<% end %>
				<% if current_user.role?(:assets_site) %>
				<li class="nav-item m-tabs__item">
					<a class="nav-link m-tabs__link " data-toggle="tab" href="#request_tab19_content" role="tab">
						Received &nbsp;
						<% if @request_assets.by_site_user_6(current_user.id).count > 0 %>
						<span class="m-badge m-badge--success"><%= @request_assets.by_site_user_6(current_user.id).count %></span>
						<% end %>
					</a>
				</li>
				<% end %>

			</ul>
		</div>
	</div>
	<div class="m-portlet__body">
		<!--begin::Content-->
		<div class="tab-content">

			<% if current_user.role?(:assets_province) %>
				<div class="tab-pane active" id="request_tab1_content" aria-expanded="true">
					<div class="m-widget5">
						<% if @request_assets.by_province_user_0(current_user.id).count > 0 %>
							<% @request_assets.by_province_user_0(current_user.id).each do |request_asset| %>
								<% if request_asset.provincemanager? %>
									<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
								<% elsif request_asset.sitemanager? %>
									<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: true  %>
								<% end %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Opened Requestes now - </p>
					<% end %>
					</div>
				</div>
			<% end %>
			<% if current_user.role?(:assets_site) %>
				<div class="tab-pane active" id="request_tab2_content" aria-expanded="true">
					<div class="m-widget5">
						<% if @request_assets.by_site_user_0(current_user.id).count > 0 %>
							<% @request_assets.by_site_user_0(current_user.id).each do |request_asset| %>
								<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>	
							<% end %>
						<% else %>
							<p class="text-center"> - No Opened Requestes now - </p>
						<% end %>
					</div>
				</div>
			<% end %>

			<% if current_user.role?(:assets_manager) %>
				<div class="tab-pane active" id="request_tab3_content" aria-expanded="false">
					<div class="m-widget5">
						<% if @request_assets.pass.count > 0 %>
							<% @request_assets.pass.each do |request_asset| %>
								<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: true  %>	
							<% end %>
						<% else %>
							<p class="text-center"> - No Pass Requestes now - </p>
						<% end %>
					</div>
				</div>
			<% end %>
		<% if current_user.role?(:assets_province) %>
			<div class="tab-pane" id="request_tab4_content" aria-expanded="false">
				<div class="m-widget5">
					<% if @request_assets.by_province_user_1(current_user.id).count > 0 %>
						<% @request_assets.by_province_user_1(current_user.id).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Pass Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>
		<% if current_user.role?(:assets_site) %>
			<div class="tab-pane" id="request_tab5_content" aria-expanded="false">
				<div class="m-widget5">
					<% if @request_assets.by_site_user_1(current_user.id).count > 0 %>
						<% @request_assets.by_site_user_1(current_user.id).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Pass Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>

		<% if current_user.role?(:assets_province) %>
			<div class="tab-pane" id="request_tab6_content" aria-expanded="false">
				<div class="m-widget5">
					<% if @request_assets.by_province_user_2(current_user.id).count > 0 %>
						<% @request_assets.by_province_user_2(current_user.id).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Not Pass Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>
		<% if current_user.role?(:assets_site) %>
			<div class="tab-pane" id="request_tab7_content" aria-expanded="false">
				<div class="m-widget5">
					<% if @request_assets.by_site_user_2(current_user.id).count > 0 %>
						<% @request_assets.by_site_user_2(current_user.id).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Not Pass Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>

		<% if current_user.role?(:assets_manager) %>
			<div class="tab-pane" id="request_tab8_content" aria-expanded="false">
				<div class="m-widget5">
					<% if @request_assets.accepted.count > 0 %>
						<% @request_assets.accepted.each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Accepted Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>

		<% if current_user.role?(:assets_province) %>
			<div class="tab-pane" id="request_tab9_content" aria-expanded="false">
				<div class="m-widget5">
					<% if @request_assets.by_province_user_3(current_user.id).count > 0 %>
						<% @request_assets.by_province_user_3(current_user.id).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No assepted Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>


		<% if current_user.role?(:assets_site) %>
			<div class="tab-pane" id="request_tab10_content" aria-expanded="false">
				<div class="m-widget5">
					<% if @request_assets.by_site_user_3(current_user.id).count > 0 %>
						<% @request_assets.by_site_user_3(current_user.id).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Accepted Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>


		<% if current_user.role?(:assets_manager) %>
			<div class="tab-pane" id="request_tab11_content" aria-expanded="false">

				<p class="text-center"> - Last 5 Refused request - </p>

				<div class="m-widget5">
					<% if @request_assets.refused.count > 0 %>
						<% @request_assets.refused.limit(5).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Refused Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>


		<% if current_user.role?(:assets_province) %>
			<div class="tab-pane" id="request_tab12_content" aria-expanded="false">

				<p class="text-center"> - Last 5 Refused request - </p>

				<div class="m-widget5">
					<% if @request_assets.by_province_user_4(current_user.id).count > 0 %>
						<% @request_assets.by_province_user_4(current_user.id).limit(5).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Refused Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>


		<% if current_user.role?(:assets_site) %>
			<div class="tab-pane" id="request_tab13_content" aria-expanded="false">

				<p class="text-center"> - Last 5 Refused request - </p>
				
				<div class="m-widget5">
					<% if @request_assets.by_site_user_4(current_user.id).count > 0 %>
						<% @request_assets.by_site_user_4(current_user.id).limit(5).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Refused Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>


		<% if current_user.role?(:assets_manager) %>
			<div class="tab-pane" id="request_tab14_content" aria-expanded="false">
				<div class="m-widget5">
					<% if @request_assets.ready.count > 0 %>
						<% @request_assets.ready.each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Ready Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>


		<% if current_user.role?(:assets_province) %>
			<div class="tab-pane" id="request_tab15_content" aria-expanded="false">
				<div class="m-widget5">
					<% if @request_assets.by_province_user_5(current_user.id).count > 0 %>
						<% @request_assets.by_province_user_5(current_user.id).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Ready Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>


		<% if current_user.role?(:assets_site) %>
			<div class="tab-pane" id="request_tab16_content" aria-expanded="false">
				<div class="m-widget5">
					<% if @request_assets.by_site_user_5(current_user.id).count > 0 %>
						<% @request_assets.by_site_user_5(current_user.id).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: true  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Ready Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>


		<% if current_user.role?(:assets_manager) %>
			<div class="tab-pane" id="request_tab17_content" aria-expanded="false">

				<p class="text-center"> - Last 5 Received Request - </p>

				<div class="m-widget5">
					<% if @request_assets.received.count > 0 %>
						<% @request_assets.received.limit(5).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Received Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>


		<% if current_user.role?(:assets_province) %>
			<div class="tab-pane" id="request_tab18_content" aria-expanded="false">

				<p class="text-center"> - Last 5 Received Request - </p>

				<div class="m-widget5">
					<% if @request_assets.by_province_user_6(current_user.id).count > 0 %>
						<% @request_assets.by_province_user_6(current_user.id).limit(5).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Received Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>


		<% if current_user.role?(:assets_site) %>
			<div class="tab-pane" id="request_tab19_content" aria-expanded="false">

				<p class="text-center"> - Last 5 Received Request - </p>

				<div class="m-widget5">
					<% if @request_assets.by_site_user_6(current_user.id).count > 0 %>
						<% @request_assets.by_site_user_6(current_user.id).limit(5).each do |request_asset| %>
							<%= render 'item_request', request_asset: request_asset, request_status: request_asset.request_status, can_edit: false  %>
						<% end %>
					<% else %>
						<p class="text-center"> - No Received Requestes now - </p>
					<% end %>
				</div>
			</div>
		<% end %>



		</div>
		<!--end::Content-->
	</div>
</div>